Day 1 - [09/07/2026]

Built: 1 switch 4 PCs with static IP addressing
Tested: Ping between all PCs was successful
Issues: None

Day 2 - [10/07/2026]

Built: Connected R1 and R2 using GigabitEthernet0/0
Configured: Set hostnames on R1 and R2 based on the network diagram
Configured: Set the enable password to cisco on both routers
Tested: Checked the running configuration and saw the password in plain text
Configured: Enabled service password encryption on both routers
Tested: Checked the running configuration again and confirmed the password was encrypted
Configured: Disabled service password encryption on both routers
Tested: Checked the running configuration again and confirmed the existing encrypted password stayed encrypted
Issues: None

Day 3 - [12/07/2026]

Built: Configured the enable password as cisco and the enable secret as ccna on R1 and R2
Tested: Entered privileged EXEC mode and confirmed only the enable secret worked
Tested: Checked the running configuration and confirmed the enable secret was encrypted while the enable password stayed in plain text until service password encryption was enabled
Configured: Enabled service password encryption and confirmed the enable password became encrypted
Saved: Used the write command to save the configuration and reloaded the router to confirm the settings were saved
Issues: The write command failed at first because I was in global configuration mode. I exited to privileged EXEC mode and tried again

Day 4 - [14/07/2026]

Built: Connected PC1 to R1 using the console cable
Configured: Set the console password to ccna on line console 0 and enabled login
Tested: Verified the console password was required before accessing the CLI
Configured: Enabled service password encryption and confirmed the console password was encrypted in the running configuration
Tested: Skipped the System Configuration Dialog and entered the CLI directly
Saved: Used the write command to save the configuration successfully
Issues: Forgot to use the login command at first so the console password was not enforced. Also tried the write command while still in configuration mode and had to return to privileged EXEC mode

Day 5 - [27/07/2026]

Built: Created two VLANs on two switches with four PCs
Configured: Assigned PC1 and PC3 to VLAN 1
Configured: Assigned PC2 and PC4 to VLAN 2
Tested: Ping between PC1 and PC3 was successful
Tested: Ping between PC2 and PC4 failed
Learned: VLAN 2 devices could not communicate because the link between the switches was not configured as a trunk
Configured: Changed the link between SW1 and SW2 to trunk mode
Tested: Ping between PC1 and PC3 was successful
Tested: Ping between PC2 and PC4 was successful
Learned: After configuring the trunk both VLANs could communicate between the switches
Issues: None

Day 6 - [28/07/2026]

Built: Connected two switches with three PCs
Configured: Assigned PC2 and PC3 to VLAN 2
Tested: Ping between the PCs before trunk configuration
Configured: Created a trunk link between SW1 and SW2 using 802.1Q encapsulation
Tested: Verified successful connectivity between devices in the same VLAN across both switches
Learned: Trunk links carry traffic for multiple VLANs between switches while VLANs keep network traffic separated
Issues: Had to configure trunk encapsulation as dot1q before enabling trunk mode because the switch was using auto encapsulation  



Day 7 - VLSM & Static Routing - [02/09/2026]
Built: Subnetted 192.168.5.0/24 using VLSM to support 4 LANs with different host requirements (45, 64, 14, and 9 hosts)
Configured: Assigned the first usable address in each LAN to the PC, and the last usable address to the router's interface
Configured: Static routes on R1 and R2 so each router could reach networks it does not directly own
Tested: Verified show ip route output showed connected (C) routes for locally owned LANs and static (S) routes for remote LANs
Tested: Pinged between PCs across different LANs - first two pings timed out, then succeeded
Learned: Initial ping timeouts were caused by ARP resolving the destination MAC address for the first time, not a misconfiguration - this is normal ARP behavior
Learned: In VLSM, subnets must be allocated largest requirement first to avoid overlapping address ranges within the same parent network
Issues: None
